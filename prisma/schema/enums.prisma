enum SubscriptionStatus {
  none
  trialing
  active
  past_due
  canceled
  unpaid
}

enum PlanType {
  free
  premium
  pro
}

enum AssetReferenceType {
  COVER
  CONTENT_IMAGE
  SNAPSHOT
  EXPORT
}
